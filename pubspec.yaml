name: estudio_app
description: "App educativa para estudiar con preguntas y respuestas"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.5.0

dependencies:
  change_app_package_name: ^1.5.0
  cloud_firestore: ^6.2.0
  firebase_auth: ^6.3.0
  firebase_core: ^4.6.0
  flutter:
    sdk: flutter
  
  # Gestión de estado
  provider: ^6.1.1
  
  # Almacenamiento local
  shared_preferences: ^2.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
     - assets/images/login_bg.jpg